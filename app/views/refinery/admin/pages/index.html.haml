.row
  .col-md-8.tree
    .box
      - cache_if(Refinery::Pages.cache_pages_backend, [Refinery::Core.base_cache_key, "pages_backend", Globalize.locale].join('_')) do
        = render 'records'

  .col-md-4
    .actions
      = render 'actions'
  
= render '/refinery/admin/make_sortable', :tree => true, :replace_contents_after_update => true, :activebackend => true if @pages.many?
